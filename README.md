# TUTUAI

- **仓库根目录**：面向 GitHub Pages 的静态站点（`index.html`、`assets/` 等）。
- **`project/`**：完整 Vite 源码（与本地开发目录一致）。进入 `project/` 后执行 `npm install`、`npm run build` 生成 `dist/`，再将 `dist/` 内文件同步到仓库根目录即可更新线上站点。
