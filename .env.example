# 可选：覆盖 Netlify 云端 API 地址（换站点或自定义域名时用）
# 不创建 .env 时，GitHub Pages 会走默认 Netlify 生产地址
# VITE_CLOUD_API_URL=https://beautiful-basbousa-c7556d.netlify.app/.netlify/functions/app-state

# 可选：云端 API 访问令牌（与 Netlify 环境变量 APP_STATE_TOKEN 保持一致）
# 未配置时 API 无鉴权；配置后前端请求须携带 X-App-Token 头
# VITE_CLOUD_APP_TOKEN=your-secret-token
